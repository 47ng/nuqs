## Review

**No security findings caused or worsened by this PR.**

- **Correct:** The changed step at `.github/workflows/ci-cd.yml:95-100` explicitly runs the docs `postinstall`, but remains in the unprivileged lint job:
  - Workflow permissions are limited to `contents: read` at `.github/workflows/ci-cd.yml:43-44`.
  - Checkout credentials are not persisted at `.github/workflows/ci-cd.yml:54-56`.
  - No secrets or untrusted GitHub expressions are interpolated into the added command.
- **Correct:** The invoked script resolves to `fumadocs-mdx` at `packages/docs/package.json:23`. Its version is exact-pinned at `packages/docs/package.json:52`, and its tarball has an integrity hash at `pnpm-lock.yaml:5853-5855`. Installation remains frozen and ignores lifecycle scripts globally at `.github/workflows/ci-cd.yml:80-81`.
- **Correct:** The generator loads `packages/docs/source.config.ts` and writes `.source` files only for subsequent Knip analysis. The lint job neither uploads these files nor passes them into publishing.
- **Correct:** The release caller passes only Turbo credentials at `.github/workflows/release-draft.yml:89-101`; the changed lint step does not expose them through its environment. Staging runs in a separate, fresh job and does not consume lint artifacts.
- **Correct:** Cache scope and keys are unchanged. The added generated files are outside the pnpm store cache, and PR caches cannot replace default-branch caches.
- **Correct:** No action reference was added or changed. Existing actions use full commit SHAs.
- **Note:** `pinact` reported missing version annotations on existing SHA-pinned actions. These lines are identical in `origin/next`, so they are pre-existing and omitted from findings.
- **Note:** No historical audit, git-history sweep, IOC scan, incident-indicator search, fetch, install, or mutating test was performed.

### Commands/checks run

- Compared workflow names and diff with `git diff origin/next...HEAD`.
- Inspected the complete workflow, base workflow, invoked package script, lockfile resolution/integrity, source configuration, generator implementation, and release caller.
- `actionlint .github/workflows/ci-cd.yml` — passed.
- `zizmor .github/workflows/ci-cd.yml` — passed with no findings.
- `pinact run --check .github/workflows/ci-cd.yml` — failed only for pre-existing missing pin annotations.
- `git diff --check origin/next...HEAD -- .github/workflows/ci-cd.yml` — passed.
- Final `git diff --cached --name-only` — empty.