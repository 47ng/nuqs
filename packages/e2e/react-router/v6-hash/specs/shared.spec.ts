/**
 * Note: HashRouter uses a different URL structure where search params
 * are stored in the hash fragment (e.g., /#/page?foo=bar).
 *
 * The shared tests use `url.search` assertions which don't work for HashRouter.
 * See hash-router.spec.ts for HashRouter-specific tests.
 */

export {}
